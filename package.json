{
  "name": "dsh-tauri-wrapper",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "check:ports": "pwsh -NoProfile -File scripts/check-ports.ps1",
    "clean:test-ports": "pwsh -NoProfile -File scripts/check-ports.ps1 -KillTest"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@tauri-apps/cli": "^1.5.0"
  }
}
